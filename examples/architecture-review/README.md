# Architecture Review Corpus

This folder is for architecture review examples only.

It is not a golden dataset, regression fixture set, or production data store.

Use these files for extraction-boundary and ingestion review, including checks around source document shape, draft/final differences, and spreadsheet ingestion assumptions.

Do not treat files here as approved expected outputs unless a future milestone explicitly promotes them into a regression fixture set.